function init(self)
	self.count = 0
end

function on_message(self, message_id, message, sender)
	if message_id == hash("click") then
		self.count = self.count + 1
		gui.set_text("#text", "Count: " .. self.count)
	end
end
